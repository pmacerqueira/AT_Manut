<?php
/**
 * MODELO — não usar este ficheiro em produção com dados reais.
 *
 * No seu sítio:
 *   1) Na pasta AT_Manut do PC, corre: npm run gen:report-auth
 *      (cria atm_report_auth.secret.php e actualiza .env.local — ambos fora do Git).
 *   2) Envia atm_report_auth.secret.php para public_html/api/ no cPanel
 *      (mesma pasta que send-email.php), ou cola o putenv dentro de config.deploy-secrets.php.
 *
 * O ficheiro real chama só putenv('ATM_REPORT_AUTH_TOKEN=...'); sem defaults fracos no código.
 */
declare(strict_types=1);

// putenv('ATM_REPORT_AUTH_TOKEN=cole_aqui_o_token_gerado');

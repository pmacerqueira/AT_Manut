<?php
/**
 * Cópia para o servidor como config.deploy-secrets.php (não commitar).
 * Preferir SetEnv em public_html/api/.htaccess (ver .htaccess no repo + ticket CiberConceito).
 * Usar este ficheiro só como fallback se mod_env / SetEnv não estiver disponível.
 * O ficheiro real está bloqueado por HTTP em .htaccess.
 */
declare(strict_types=1);

// putenv('ATM_DB_HOST=localhost');
// putenv('ATM_DB_NAME=...');
// putenv('ATM_DB_USER=...');
// putenv('ATM_DB_PASS=...');
// putenv('ATM_JWT_SECRET=...');
// putenv('ATM_TAXONOMY_TOKEN=...');
// putenv('ATM_REPORT_AUTH_TOKEN=...'); // send-email, send-report, log-receiver, cron HTTP
// putenv('ATM_WEBHOOK_TOKEN=...'); // opcional
// putenv('ATM_NAVEL_DOC_INTEGRATION_TOKEN=...'); // biblioteca Fase C

# URL base da API (servidor PHP). Ex: https://www.navel.pt
# Se vazio, usa window.location.origin (funciona em dev e produção).
# Endpoints: /api/send-report.php (emails), /api/send-email.php (PDF), /api/data.php
VITE_API_BASE_URL=
